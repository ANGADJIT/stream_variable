import 'package:flutter_test/flutter_test.dart';

import 'package:stream_variable/stream_variable.dart';

void main() {
  test('adds one to input values', () {
    final calculator = StreamVariable();
    
  });
}
